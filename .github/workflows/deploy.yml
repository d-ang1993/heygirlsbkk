      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 145.79.28.100
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21

          # 👇 The local theme folder
          local-dir: ./

          # 👇 The exact directory on Hostinger
          server-dir: /public_html/wp-content/themes/heygirlsbkk/

          # 👇 Include this flag to create directories if missing
          dangerous-clean-slate: false

          exclude: |
            **/.git*
            **/.github*
            **/node_modules/**
            **/vendor/**
            **/package-lock.json
            **/composer.lock
            **/.env
            **/composer.json
            **/package.json
            **/vite.config.js
            **/webpack.mix.js
            **/tailwind.config.js
            **/postcss.config.js
            **/babel.config.js
            **/.DS_Store
            **/Thumbs.db
            **/npm-debug.log
            **/yarn-error.log
            **/README.md
            **/LICENSE*
